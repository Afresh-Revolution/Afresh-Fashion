# Heroku / DigitalOcean App Platform (no Docker)
# Build: npm ci && npm run build
# Run:    npm start

name: afresh-fashion

services:
  - name: web
    http_port: 8080
    build_command: npm ci && npm run build
    run_command: npm start
